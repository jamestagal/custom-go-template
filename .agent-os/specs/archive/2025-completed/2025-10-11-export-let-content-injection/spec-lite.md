# Spec Summary (Lite)

Implement Svelte-compatible `export let` syntax for declaring props that should be populated from external JSON content files. This enables data-driven templates where content lives in `content/*.json` files and templates declare which props to receive using familiar `export let propName` syntax, providing the foundation for Plenti compatibility.
